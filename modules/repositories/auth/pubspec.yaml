name: auth
description: A module for Auth related actions.
version: 0.0.1

publish_to: "none"

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=2.10.2"

dependencies:
  core:
    path: ../../core
  flutter:
    sdk: flutter
  freezed_annotation: ^1.1.0
  json_annotation: ^4.4.0
  meta: ^1.7.0
  network:
    path: ../../network
  rxdart: ^0.27.3
  user:
    path: ../user

dev_dependencies:
  build_runner: ^2.1.7
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  freezed: ^1.1.1
  json_serializable: ^6.1.4

flutter: null
